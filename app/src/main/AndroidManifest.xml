<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.games4science.truerandommusicplayer"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- ========================= -->
    <!--        PERMISSIONS        -->
    <!-- ========================= -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" /> <!-- Android 13+ notifications -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" tools:targetApi="34" /> <!-- Required for media playback foreground services (Android 14+) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- Keeps CPU awake during playback -->
    <!-- ========================= -->
    <!--        APPLICATION        -->
    <!-- ========================= -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrueRandomMusicPlayer">
        <!-- ========================= -->
        <!--        MAIN ACTIVITY      -->
        <!-- ========================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- ========================= -->
        <!--  MANAGE PLAYLIST ACTIVITY -->
        <!-- ========================= -->
        <activity
            android:name=".ManagePlaylistsActivity"
            android:exported="false" />
        <!-- ========================= -->
        <!--        MUSIC SERVICE      -->
        <!-- ========================= -->
        <service
            android:name=".player.MusicService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <!-- Required for lock screen & headset controls -->
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <!-- For Legacy/Bluetooth/Car compatibility (Recommended) -->
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
    </application>
</manifest>